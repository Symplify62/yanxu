# 长录音末尾时间戳越界修复

2026-09-21 用户授权“修”。对象：32分47秒录音 `61888bf2-64e8-4669-bd9c-f8cc568a1b34`。本机原音SHA与上传清单一致，38段中37段缓存完成；末段1935.5–1967.0秒，最后一词31.36–31.52秒，实际段长31.5秒，越界20ms触发`Invalid or unordered word alignment`。单独重跑末段已复现；原音、37段缓存与诊断末段原始识别结果均保留在Git忽略的数据目录。

## 行为与验证合同

- 仅容忍结束时间超过片段末尾不大于20ms的情况，裁至真实片段末尾；起点必须仍在片段内。倒序、负数、非有限时间、超过容差等继续拒绝。保留原始词级时间，并记录修正前后值及原因。
- 先添加该真实边界的失败测试，再修实现；覆盖阈值内/外、倒序、空输入、原始数据不被修改、修正可追溯及正常文本保留。
- 源码指纹变化会使旧ASR目录拒绝直接续跑。恢复前校验原清单/每段指纹、音频SHA、模型与运行参数；将原失败目录完整归档，重新校验和生成缓存时间轴/指纹，复用37段及诊断阶段已得到的末段识别，不伪造识别正文。
- 只重试这个已失败的云任务，保持记录ID和原音不变；验证Mac提交、云端DeepSeek分析完成、公共接口状态和结果。其他记录不操作。
- 可见UI改动：不涉及；已有页面的真实业务状态通过接口与页面读回确认。未授权提交Git。

## 结果

- 失败用例在原实现上复现`Invalid or unordered word alignment`；修复后真实末段129个词成功生成7个时间片，最终结束时间1967.0秒。保留原始31.52秒值，唯一修正为31.50秒。
- 后端30项测试全部通过（原22项＋8项时间边界测试）。没有放宽负数、倒序、非有限值、起点越界和超过20ms的异常。
- 恢复脚本校验输入/标准化音频SHA、原清单与每段指纹、模型内容hash、Python/库版本和完整VAD分段；只允许本次artifacts.py/transcribe.py的已知变化。原失败目录完整归档在`.local-data/remote-cloud/asr-backups/61888bf2-64e8-4669-bd9c-f8cc568a1b34-before-boundary-fix`。
- 37段原缓存与诊断时得到的末段结果重新生成时间轴，正文和原始词时间未改。经Workbench以服务用户执行现有本地运维命令，仅对目标失败记录调用`yanxu.manage retry`；没有新增公开重试接口。
- Mac实际处理清单：`complete`，38/38段完成、`resumed_chunks=38`、`new_chunks=0`，约8.67秒后成功提交云端。云端DeepSeek继续处理后，公共记录为`complete`、错误清空。
- 公共接口读回：原时长1967秒、原音SHA保持一致；转写9621字符、426个时间片，最后结束1967.0秒；AI摘要188字符、9个重点、8个行动事项。页面实际展示同一记录与188字符摘要。生成数量不代表语义准确性已人工审校。
- 脱敏证据见[恢复结果](../../backend/evidence/asr-boundary-recovery.json)。原始识别、审计修正和恢复脚本保留在Git忽略的`.local-data/diagnostics/asr-last-chunk`，不提交会议内容。代码未提交。
