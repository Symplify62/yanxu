# 第一阶段真实接入实施记录

2026-09-21用户已填写DeepSeek key并要求继续。范围：本地持久后端、Qwen转写、DeepSeek真实分析、公共结果/原音播放下载，Android原生采音App与本地接口联调。不增加登录/角色/群推送，不部署公网，不自动提交。

## 实施与验证合同

- 本地单Mac选择Python API＋独立worker＋SQLite WAL事务队列＋持久文件。SQLite为本地部署选择，替代原“PostgreSQL候选”但不宣称已支持多主机队列。
- 上传按固定块、稳定client_id、全文件SHA及受控上传令牌实现幂等；上传令牌仅保护某次写入，不是用户身份登录。匿名公开列表/详情/音频无token。
- worker持久领取、租约/重试、阶段结果保留；ASR本机禁网；DeepSeek只接收转写并验证JSON，key不出后端。长文本分段分析后汇总，不静默截断。
- Android原生AudioRecord＋麦克风前台服务＋本地PCM/WAV保存＋JobScheduler后台上传。调试包先模拟器；已连手机不自动录音。
- 新增后端/Android模块，现有模拟页面保留；公开真实Web入口单独增加并复用A页面。真实列表中不混入mock记录。
- 验证：API幂等/分片/哈希/原音范围请求/失败与任务恢复单测，真实本地样本→本地ASR→DeepSeek→公共查询闭环；刷新与服务重启保留；Web桌面/手机截图；Android构建、安装及模拟器录音保存/上传证据。物理手机拾音、锁屏长期和真实公网单独报告。

当前：DeepSeek /models已验证key可用，未输出key。后续结果按实际证据补齐。

## 实际完成与验证

- 本地API 5189＋worker已运行，SQLite/资产位于`.local-data/server`。先以2分01秒真实A1样本验证，再从Android系统文件选择器导入同一样本，经过App分片上传→Qwen本机禁网ASR→DeepSeek真实JSON分析→匿名公共结果，均完成。
- App导入记录`483956de-46fa-4eb8-9b41-4b907da453ec`可在公共结果查看；原音下载SHA与上传源文件一致。真实转写15个时间片段，不使用固定页面样例；无明确事项时保留空数组。
- API及worker实际重启后已保存记录/结果仍可读；模拟租约过期、旧worker迟到、阶段失败重试由自动测试验证。没有据此宣称所有故障组合已真机验证。
- Android debug 0.1.2已构建，在专用API36模拟器验证开始/暂停/继续/结束、本地WAV、自动上传；模拟器无清晰语音时后端正确报转写未完成，原音仍可读。真实音频导入在App内完成，无需再人工点击转写/分析。
- 已连接的ANA-AN00（SDK31）手机已安装同一测试包，并配置USB `adb reverse` 到5189。没有自动授予或开启该手机麦克风；物理拾音、锁屏/来电、长录音、OEM后台和后续平板未验收。
- Web真实入口支持匿名列表/搜索/筛选/分页、分阶段结果、原音播放器、Range和下载；播放metadata与时长检查、播放时间推进、下载校验及刷新已通过。App公共结果页读取同一服务。真实样本截图放`.local-data/evidence`而非Git。
- 新鲜检查：后端10项协议/文件/任务/预算/恢复测试、既有30项模型/HTTP测试、6项原型回归和1项真实服务浏览器E2E通过；前端构建/格式与Android构建通过。Python测试依赖有弃用提示，不是失败。
- 当前资源边界、运行/恢复命令见[后端说明](../../backend/README.md)，App构建/连接见[Android说明](../../android/README.md)。原型5178保留，真实服务为5189。没有推送或公网部署，本次未提交。

## 接手入口

云存储接入已在同日继续完成，当前配置与验证见[七牛接入记录](qiniu-storage.md)。下列本地 ASR/API 入口仍有效；新录音自动同步七牛，旧本地会议未批量迁移。

1. 保留当前已填key的`config/.env.local`，不要覆盖成模板或输出其值。API和worker由backend/README中的两条启动命令运行；模型环境在`~/.local/share/pcim-asr`。
2. 先检查5189健康与现有进程归属，复用当前服务。真实数据与token只在`.local-data/server`，Git内仅有[脱敏结果记录](../../backend/evidence/live-result-metadata.json)。
3. 手机USB保持连接时App默认`http://127.0.0.1:5189`经adb反向端口访问Mac。拔线后须重新配置可达服务地址，不能误认为互联网已经部署。
4. 尚未关闭的验证：物理手机录音/后台/长期恢复、跨网络公网、对齐精度和多人说话人分离。ASR选型时的S3低覆盖问题仍保留；当前worker会对明显低覆盖/无语音报错，而不生成假摘要。
