# 根仓库初始化与AI文档导航验证

日期：2026-09-21。用户授权：项目建立Git、提交一次当前代码、增加AGENTS与前端文档渐进式导航。此次是仓库与文档交付，不改变UI或业务行为，不推送远程。

## 预编辑检查与完成条件

- 项目根无Git；frontend/.git无提交、无远程、无已跟踪文件，现有工程均属本项目原型。本次将完整项目初始基线一次提交。
- 备份frontend空仓库元数据到.local-backups/repo-init-20260921/frontend.git，再初始化根仓库；源码位置不变。依赖/构建/缓存/备份不进入提交。
- 项目AGENTS只维护当前范围、关键约束和按任务阅读路由；前端指向专题目录及具体规范，不要求每次全读。
- 新规范依据A实际实现、v1.3页面/状态/权限规格整理，区分已实现、设计目标、未验收；消除当前DES-05中的旧选型语句。历史文档作为历史保留。
- 验证：文档链接与可达性、入口大小、8类任务导航样例；构建、格式、单测与新鲜现有E2E；提交前检查暂存文件、敏感内容和嵌套gitlink；提交后确认单个根仓库、提交内容及工作区状态。
- 不涉及本轮可见UI变化；沿用既有页面并回归，不新增视觉验收声明。

结果在首次提交前补全；提交哈希以Git记录为准，不在本提交内自引用。

## 验证结果

- 单一根仓库已建立：main分支；frontend从根目录解析Git，不再有嵌套.git。原空仓库和旧图谱均已本地备份，无远程配置。
- 根AGENTS 30行，前端AGENTS 16行；21份导航文档、163个本地链接、8类任务路由结构检查通过，前端6专题均在根入口2次链接内可达。人工核对触发条件与专题职责；结构检查不等于LLM语义正确性评估。
- 当前DES-05改为权威专题路由，当前START_CODEX与v1.3入口改为按任务读取；历史版本不作为当前执行指令。前端CLAUDE只转到同一AGENTS，避免双套规则。
- v1.3原有3项文档校验均通过：96需求、128产品验收规格、21页55组件；产品规格本身仍不等于真实产品测试通过。
- 前端构建/类型、格式检查通过；30项模型/HTTP测试和27项页面E2E通过（3.1分钟，无跳过/波动）。本轮没有改动应用行为。
- 根GitNexus索引已建立；动态/跨语言字段存在工具覆盖限制，后续按文件限定并补源码/测试，不将空调用者视为无影响。
- 暂存检查：无gitlink或符号链接，无依赖/构建/缓存/备份目录泄漏；只纳入两份mock环境预设。文本凭证特征扫描无命中，不声称已对历史二进制资料做内容安全审计。
- 初始资料/日志中12处文件末尾多余空行机械规范化，正文与代码行为未变；暂存空白检查通过。

新鲜结果保存在frontend/evidence/repository-init目录。当前可用性为本地源码＋已验证的模拟Web原型；未设置远程、未推送或发布。Git提交与提交后状态在交付时从仓库读取。
