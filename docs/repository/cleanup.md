# 仓库清理与验证（2026-09-21）

本次删除过时来源并收敛前端依赖，不改变录音、上传、转写、分析和公共资料的业务行为，不部署服务器或更新安装包。

## 范围与完成条件

- 旧 PRD v1.0–v1.2、根目录重复准备度核对记录、旧单文件 HTML 从当前分支移除，历史仍可从清理前提交 `fee7f7a` 找回。四个旧 ZIP 已通过 CRC 完整性检查并移至 `/Users/alec/言序历史交付归档/2026-09-21/`，不删除原始交付包。准备度核对的历史副本仍在 v1.3/evidence。
- 保留 v1.3 的完整产品规则、A 方案原型、第一阶段演示及它们仍有意义的回归测试。移除 B/C 比较入口、专用组件和仅被它们使用的依赖。
- 公共结果页与 A 共享样式具有明确目录，真实入口不再导入设计比较或演示实现；演示样例由演示入口提供。
- 导航明确当前真实服务、演示、后续完整方案三者；过时截图和重复运行报告留在 Git 历史，新鲜测试产物写入被忽略的本地证据目录。
- `.local-data`、`.local-backups`、配置密钥、录音、数据库、模型及服务进程保持原状；仅测试产生新的本地证据。

## 影响与验证约定

GitNexus 已刷新根仓库索引。`stageOf` 直接影响列表筛选与详情进度；`useKit` 影响完整 A 原型的员工、后台、登录及组件页。Vue 模板、动态加载和 CSS 的依赖另用源码导入、构建入口和浏览器测试补证；MCP 缓存报告旧提交时采用本机 CLI 的新索引结果。

可见变化仅是 A 原型移除已淘汰的方案切换/历史比较入口；真实公共页面外观与交互应保持不变。

完成前运行：文档链接与导航检查、前端类型/构建/单测、A 原型与第一阶段演示的 Playwright 回归、真实本地 API 的公共列表/详情/回听/下载及窄屏固定筛选栏回归。保留本轮截图并目视检查受影响代表页。检查 Git 差异与删除清单后仅提交本任务内容。

## 结果

- 当前受 Git 跟踪文件由 724 个降至 350 个，文件内容由约 22.08 MiB 降至 7.43 MiB；删除 376 个文件，另有 5 个迁移、2 个新增。数值指当前文件树，不包含 Git 历史、依赖及运行数据。
- 前端移除 9 个直接依赖声明，依赖树减少 40 个包；本机 node_modules 由约 548 MB 降至 271 MB。保留 Vant/Vue Router，因为早期回归页面仍使用它们。
- 完整 A 原型移除历史比较链接；公共页面不再导入 design-lab/phase-one 实现。详情的模拟文字与分析由演示入口显式传入，真实页面仍只显示 API 资料。
- v1.3、A 原型、第一阶段演示、早期仍共用服务的组件回归保留；运行配置、后端、Android、部署脚本没有代码变更。

验证结果：

| 检查 | 本次结果 |
| --- | --- |
| `python3 tools/check_docs.py` | 32 份导航文档、249 个本地链接、9 个任务路由通过 |
| 全部受跟踪 Markdown 的本地链接巡检 | 0 个缺失目标 |
| `npm run build`（含 vue-tsc）/ `npm run format:check` | 通过 |
| `npm test` | 30 项通过 |
| `npm run test:e2e` | 22 项通过，约 2.4 分钟；A/第一阶段/早期组件流程均覆盖 |
| 清理 A 外壳剩余无用样式后，重建并复测 design-selection | 2 项通过 |
| `npm run test:live` | 清理前后各 7 项通过；后测使用新构建与本机真实 API |
| GitNexus staged impact | medium，4 条原型 boot/reset/seed 流程；已由原型回归覆盖 |
| `git diff --check` | 通过 |

本次截图/构建/影响记录位于 `.local-data/evidence/repository-cleanup/`，真实 API 检查位于 `.local-data/evidence/web/`。已目视核对 A 外壳与窄屏结果详情，桌面列表与详情截图哈希和清理前一致。手机截图未逐像素一致，差异集中在结果链接按钮的截取状态，未将原因视为已证实；已目视核对，宽度与交互断言通过。真实服务测试核对原音播放进度、Range 返回、下载 SHA256 与刷新详情。

本轮 22 项完整 E2E 的结果来自执行输出；同目录 JSON 在最后 2 项 A 入口复测时被覆盖，仅代表最终入口复测，不能作为 22 项报告。后续原型测试统一输出到 `.local-data/evidence/frontend/`，避免修改仓库中的历史证据。

本次属于源码清理与本地验证，不涉及重新录音、重新转写、真机专项验收、服务器部署或 APK 更新。功能分支为 `feat/repository-cleanup`；推送、PR 合并与部署分别处理。

## 历史恢复

旧版本可从 `fee7f7a` 查看或按需导出到项目外，例如 `git show fee7f7a:<历史文件路径>`；不把整套旧目录恢复到当前工作树当现行规则。四个 ZIP 的项目外归档路径见上方范围。当前分支删除不清除 Git 历史。
