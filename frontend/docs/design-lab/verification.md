# 三套正式组件样板验证合同（2026-09-21）

> 历史时点记录：其中已淘汰的比较实现、旧截图和运行报告从清理前提交 `fee7f7a` 查询；当前结构与验证见[仓库清理记录](../../../docs/repository/cleanup.md)。


授权：用户接受对同一平板扫码页进行 Element Plus 精修、shadcn/vue、TDesign 三套实际组件对比。只交付独立可点击样板，不冻结设计、不迁移全产品。

变更范围：新增 design-lab.html 比较入口、design-option.html 隔离页面及 src/design-lab；新增相关依赖和 Vite 多页入口。现有 /tablet、/employee、/admin、业务状态机和 MSW 服务不改。每个样板使用独立文档和模拟数据，切换方案重新开始。

影响：GitNexus router 上游没有解析到调用者，风险 UNKNOWN；源码确认 main.ts 引入 router。但最终选择独立 HTML 入口，无需修改 router/App。Vite 构建配置影响所有入口，需构建及旧流程回归。新页面复用现有 device API，不能绕过其权限和状态校验。样式以 iframe 隔离。

设计验证：三方案同信息、同流程、不同布局。使用真实 Element/TDesign 组件以及官方 shadcn/vue registry 源码；不以图片代替正式组件。二维码为明确标识的演示数据，不是真实认证；模拟控制位于产品区域外。

完成条件：三方案可切换，独立弹窗可取消/确认；员工扫码→授权部门→开始→暂停/继续→结束→保存后退出→下一场；过期码拒绝扫描可刷新；保存失败可恢复；保存页无手机跳转入口。桌面/平板/窄屏无横向溢出。现有30个模型/HTTP测试与6个E2E回归通过。对每方案采集真实浏览器截图并检查布局。

边界：模拟身份/音频/自动任务；不接入真机、企业微信或服务端，不发消息。不提交、不推送。当前仓库无提交且全部文件未跟踪，保留原状。

验证结果待填。

## 验证结果（完成）

- 构建与类型检查通过：evidence/design-lab/build.log。
- 格式检查通过：evidence/design-lab/format.log。
- 30项模型/HTTP测试通过：evidence/design-lab/unit-results.json。
- 全量16项E2E通过（新增10项 + 原有6项），耗时1.6分钟：evidence/design-lab/e2e-results.json 与 e2e.log。
- 最后修复「再次点击当前方案会清除就绪状态」后，补强并单独复测容器切换/重置/重复点击，1项通过：parent-retest.json。修改限于Lab.select，图谱UNKNOWN由模板@click唯一调用处补充，其他业务代码不变。
- 初轮3项失败来自测试定位差异：Element输入框由选择器外层接收点击；TDesign隐藏弹窗保留DOM；标签可访问名称带空格。修正为可见控件/隐藏状态/稳定名称后对应异常与交互均通过，原始失败记录保留在e2e-first-attempt.*。
- 三方案真实浏览器截图：element/shadcn/tdesign-{1280,768,390}.png；Ego space40 对比页及A/B/C弹窗截图 ego-lab-*.png、ego-dialog-*.png；B准备/录音/保存截图 ego-ready-b.png、ego-record-b.png、ego-saved-b.png。
- 已逐张观察三套横竖屏与390宽度：无横向溢出；C窄屏孤立字断行已修正后重拍；二维码和主操作区域可见；弹窗等待动画结束后检查；保存页只提供下一场扫码。
- 保留所有原主原型和业务文档；未提交、推送、发布，未操作真实外部系统。设计最终选择仍待用户判断，不以自动化测试替代审美选择。

本轮新增文件：design-lab.html、design-option.html、src/design-lab/**、tests/e2e/design-lab.spec.ts、docs/design-lab/**、evidence/design-lab/**。
本轮修改已有文件：package.json/package-lock.json、vite.config.ts、README.md；组件声明/构建/测试产物由工具更新。未修改主原型路由、App或业务状态机。

# A/B完整原型对比扩展（2026-09-21）

用户授权：完整对比，只保留A、B。范围为当前v0.4已有功能：平板全部录音状态；员工登录、列表/搜索/详情五标签、更正、共享/撤销、快照；管理登录、概览、用户启停、组织同步、预置角色查看、访问组编辑、部门接收群/停发、异常恢复、审计、设备；基础组件状态。

不扩展未实现的生产能力或自定义角色委派。本轮两个方案都以实际Element Plus/shadcn组件实现输入、选择、按钮、弹窗和表格；布局可按业务端变化。现有主原型不改动。仅比对页去掉C入口，保留旧C源码供历史回溯。

影响：boot图谱LOW，select因Vue模板调用缺边显示UNKNOWN，已核对Lab模板调用。复用API与模拟状态机，新增独立页面/账户状态，不改核心服务/权限规则。扩展Dialog标题是兼容可选prop。样式仍在iframe隔离。

验证合同：两方案分别完成录音→手机资料、更正不重发、共享/撤销、账号拒绝、后台配置/组编辑/异常恢复；后台每模块可访问且无会议正文；所有主要页真实截图；390px手机及1280px后台无整体横向溢出。30模型/HTTP测试及原6E2E回归，新增A/B完整流程E2E，旧平板样板测试继续。构建/类型/格式通过。状态提示与行为用同API校验。切换方案重新开始该方案模拟数据，同一方案内部切端保留模拟会议。

## A/B完整对比验证结果

已完成本轮范围。类型/构建和格式检查通过；30项模型与HTTP测试通过（full/unit-results.json）。全量25项页面E2E通过，无跳过/失败/波动（full/e2e-results.json，2.9分钟）。最终工具栏收尾后，后台A/B流程、自动恢复到渠道接收、对比容器共4项复测通过（full/final-retest.json）；另补强嵌入页内点击会议并进入详情的路径，1项通过（full/iframe-retest.json）。

本轮截图65张，覆盖两个方案的登录、列表/空态、详情五标签、更正/共享/只读/快照、后台9模块和关键弹窗、组件页以及768/390宽度。实际查看了A/B的手机列表与详情、长表单、后台概览、人员、组织、成员弹窗、接收群、异常与设备页，以及窄屏截图；结合Ego space41操作及Playwright点击验证。原主原型6项E2E保留并通过。

初轮完整流程23项中22通过，Element成员复选框测试误点被组件隐藏的原生input；改为点击可见label并断言选中后，全量复测通过。截图检查后调整长表单弹窗为标题/操作区固定、正文滚动。补上比较入口加载就绪保护、管理会话失效时清除弹窗与数据，并通过对应测试。Ego跨iframe自动滚动曾未滚动外层页面，确认元素位于视口下方后正常滚动并点击进入详情；补充Playwright嵌入点击路径验证通过，不是应用点击失败。

实现范围：新增full/页面、账户状态、管理视图模型、组件接口和kit输入/复选/表格适配；新增官方shadcn registry组件并保留原件与许可；Lab只保留A/B并增加端切换/故障场景；option入口挂载完整候选界面。未修改原业务状态机/API、原主原型页面、历史PRD或外部系统；未提交或发布。

“完整”指当前v0.4已有原型功能的一致对比。自定义角色委派、真实身份、录音/音频播放、真机可靠性、AI服务和外部发群仍未实现。视觉候选尚未最终选定，自动化检查不替代产品审美选择。

# A方案确定与入口收口

授权：用户「用A吧」。固定A正式入口、记录决策、更新默认文档入口；历史B/C代码和旧原型不删除，不扩展后端或生产能力。

预编辑验证合同：Lab新增兼容可选confirmed模式（旧比较模式默认不变），新prototype.html入口及Vite多页配置；main.boot根地址跳转。图谱boot为LOW；Lab.select为UNKNOWN，源码可见仅模板点击调用，确认版隐藏切换且select拒绝变更。验证构建/类型/格式、新入口从根地址进入A，直接打开手机/后台、录音跨端保留、正式入口无A/B控件；旧比较入口回归。保留本轮截图。原型全部为mock，不视为上线。

A确认收口结果：构建/类型与格式检查通过。7项针对性E2E通过，包含A完整业务流程、权限/共享、后台配置与恢复、登录失效、旧原型主流程以及2项新入口测试。修正正式入口页脚的历史比较说明后，2项入口测试再次通过。证据：selected/e2e-results.json、selected/final-retest.json、selected/build.log、selected/format.log；平板/手机/后台截图在selected目录，已检查正式入口无A/B切换且A页面正常呈现。

设计确认记录为selection.md；项目README、frontend README、技术选择与DES-05同步指向已确认A。未改业务状态机，未提交/发布，也未写入个人记忆库。
